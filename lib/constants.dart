const String selectButton = "SELECT";
const String removeButton = "REMOVE";
const String selectedTitle = " - Selected";
const String listPageTitle = "Items";

String getSelectedTitle(String title) {
  return title + selectedTitle;
}